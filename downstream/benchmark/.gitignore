# 生成的评测产物（split_manifest 等小元数据可手动 git add -f）
outputs/**
!outputs/.gitkeep

# baseline 第三方仓库（以 submodule / 单独 clone 管理，不纳入本仓）
baselines/*/
!baselines/.gitkeep
!baselines/README.md

# AppleDouble / 缓存
._*
__pycache__/
*.pyc
